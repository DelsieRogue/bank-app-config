server:
  port: 8082
spring:
  mvc:
    servlet:
      path: /gateway-service
  application:
    name: gateway-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/ 
